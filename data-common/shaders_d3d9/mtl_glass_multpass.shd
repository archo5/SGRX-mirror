float2 texcoord = float2( dot( T, viewPos ), dot( B, viewPos ) );
float4 diff = tex2D( Tex0, texcoord ); // * gInstanceData[0]; // TODO
float3 normal = tex2D( Tex1, texcoord ).rgb * 2 - 1;
outEmissiveLight = diff.rgb;
outOpacity = diff.a;
outDiffuseColor = float3(0,0,0);
outSpecularColor = float3(0,0,0);
outNormal = inNormal;//mul( normal, TBN );
