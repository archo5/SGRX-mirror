
function unpack_vec3( v )
{
	x = v.x; y = v.y; z = v.z;
	return { x, y, z };
}

function ENT_UPG1_marker( pos, data )
{
	data.position = unpack_vec3( data.position );
	return "Marker", data;
}
